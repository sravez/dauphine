#import "@preview/vartable:0.2.2": tabvar



#tabvar(
       variable: $x$,
       label: (([f'(x)], "s"),),
       domain: ($0$, $sqrt(n)$, $+oo$),
       contents: (
              ($-$, ($0$,"+") )
       ),
)
